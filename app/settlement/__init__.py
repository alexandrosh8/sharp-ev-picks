"""Settlement: map final scores to pick outcomes and persist results.

`outcomes` is pure math (stdlib only); `results` loads scores from free
sources; `engine` joins open picks to scores and writes result_tracking.
"""
