# Rejected alias candidates — NEGATIVE-alias suggestions (human consideration
# only; add the vetted ones to the _NOT_ADDED list in
# tests/test_resolution_nameform_aliases.py so they stay distinct forever):
# _NOT_ADDED_CANDIDATES = [
#     ('FK Shkendija 79', 'Shkendija'),  # golden_negative_conflict — [PIN-06] REJECT — GOLDEN NEGATIVE: ('Shkendija','Shkendija 79') sits in _NOT_ADDED (tests/test_resolution_nameform_aliases.py, 2026-08-03 review: 'bare shared across Balkan Shkendija clubs') and my alias normalizes to exactly that pair. Fixture evidence (UECL v NK Bravo, opponent EQ delta 0, Tetovo club founded 1979) is escalation-grade — overriding a day-old golden negative needs operator-delegated review, not a P3 batch
#     ('Neftchi', 'Neftchi Baku'),  # golden_negative_conflict — [PIN-10] REJECT — GOLDEN NEGATIVE: ('Neftchi','Neftchi Baku') was reviewed and rejected 2026-08-03 ('bare shared, Neftchi Fergana') and locked in _NOT_ADDED. My warehouse sole-user evidence (Fergona/Kochkor-Ata always qualified, UEL v Dinamo Minsk delta 0) is escalation-grade but a P3 batch does not override a golden negative
#     ('Operario', 'Operario Ferroviario'),  # golden_negative_conflict — [PIN-11] REJECT — GOLDEN NEGATIVE: ('Operario','Operario Ferroviario') was reviewed and rejected 2026-08-03 ('bare operario shared by -PR/-MS/-VG') and locked in _NOT_ADDED. The state-suffixed 'Operario-PR' form (PIN-11b) is unambiguous and approved instead; the bare form stays unmatched pending operator escalation
#     ('Athletic Club', 'Athletic Club MG'),  # contested_base — [PIN-13] REJECT — DISTINCT-ENTITY BASE CONTEST: the fixture club is Athletic Club (Sao Joao del Rei MG, Brazil Serie B) but the bare base 'athletic' is already owned by the seed's 'Athletic Club' (Bilbao) group; merging would silently fold the Serie B club into the La Liga group (CD-Nacional class). Bare form stays unmatched (errs toward NO match). If Pinnacle-side misses persist, the fix is an operator decision to restructure the Bilbao group key, not an alias
#     ('Varteks', 'Varazdin'),  # distinct_club_trap — [PIN-14] REJECT — DISTINCT CLUBS: UEFA lists the 2026-07-30 UECL fixture entity as NK Varazdin; NK Varteks (2011) is a separate fan-founded club playing Croatian lower leagues (en.wikipedia.org/wiki/NK_Varteks_(2011)). The 'Varteks' row is a stale/alternative feed label for one fixture; a permanent bare-base merge would wrong-game every future genuine NK Varteks fixture
# ]
