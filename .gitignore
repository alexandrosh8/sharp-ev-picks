# Secrets — never committed
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
build/
dist/

# Data & model artifacts (large/binary — never committed)
# Anchored to repo root so they do NOT match the app/models/ source package.
/data/
/models/
*.parquet
*.sqlite
*.sqlite3
*.db

# Logs
logs/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Notebooks checkpoints
.ipynb_checkpoints/

# Claude Code local-only settings (machine-specific)
.claude/settings.local.json
.playwright-mcp/
vendor/

# Ralph autonomous-loop scaffold + runtime (dev-local tooling, not product code)
.ralph/
.ralphrc

# Local Docker overrides (per-environment deploy topology; keep out of the repo)
docker-compose.override.yml
